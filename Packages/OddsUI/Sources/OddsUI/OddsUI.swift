import Foundation

/// OddsUI 模組的命名空間。
///
/// 依 `docs/spec.md` §11 的時程：
/// - D3（完成）：`MatchListViewController` + `MatchCell` + diffable data source
/// - D4：`MatchDetailViewController`、Debug HUD、加壓面板
public enum OddsUI {
    public static let moduleName = "OddsUI"
}
